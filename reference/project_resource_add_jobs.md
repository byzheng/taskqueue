# Add a job to project/resource

Add a job to project/resource

## Usage

``` r
project_resource_add_jobs(project, resource, job, reset = FALSE)
```

## Arguments

- project:

  project name

- resource:

  resource name

- job:

  job name. If missing, reset to empty

- reset:

  whether to reset jobs

## Value

no return
