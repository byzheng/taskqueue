# Disconnect to db

Disconnect to db

## Usage

``` r
db_disconnect(con)
```

## Arguments

- con:

  a connection object as produced by dbConnect.

## Value

no return values

## Examples

``` r
if (FALSE) { # \dontrun{
con <- db_disconnect()
dbDisconnect(con)
} # }
```
